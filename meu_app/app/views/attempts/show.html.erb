<div class="w-full max-w-xl mx-auto text-center">
  <h1 class="font-bold text-4xl mb-4">Resultado do Questionário</h1>
  <h2 class="text-2xl font-semibold text-gray-800 mb-6"><%= @attempt.questionnaire.title %></h2>

  <div class="bg-white p-8 rounded-lg shadow-xl border">
    <p class="text-lg text-gray-600 mb-2">Sua pontuação final é:</p>
    <p class="font-bold text-6xl text-blue-600">
      <%= @attempt.score %> / <%= @attempt.questionnaire.questions.count %>
    </p>
    <p class="text-gray-500 mt-1">acertos</p>
  </div>

  <div class="mt-8">
    <%= link_to "Voltar para a lista de questionários", questionnaires_path, class: "rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium hover:bg-gray-200" %>
  </div>
</div>