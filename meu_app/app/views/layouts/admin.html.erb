<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Admin Dashboard - Code Quiz" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="<%= asset_path('application.css') %>" data-turbo-track="reload">
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Navbar do Admin -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Code Quiz - Admin</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNavbar" aria-controls="adminNavbar" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="adminNavbar">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item">
              <span class="navbar-text me-3">Olá, <%= current_user.name %></span>
            </li>
            <li class="nav-item">
              <%= link_to "Dashboard", admin_dashboard_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Questionários", admin_questionnaires_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Usuários", admin_users_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Resultados", admin_results_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-outline-light" %>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Conteúdo principal -->
    <div class="container mt-4">
      <% flash.each do |key, message| %>
        <div class="alert alert-<%= key == 'notice' ? 'success' : 'danger' %> alert-dismissible fade show" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
