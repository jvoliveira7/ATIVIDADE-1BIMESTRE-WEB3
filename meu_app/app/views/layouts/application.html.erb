<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Code Quiz" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Carrega o CSS do Bootstrap compilado %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%# Carrega o JS do Bootstrap e Popper via Importmap %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-light">
    <%# --- BARRA DE NAVEGAÇÃO BOOTSTRAP COMPLETA --- %>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
          <%= link_to "Code Quiz", root_path, class: "navbar-brand" %>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
              <% if user_signed_in? %>
                <li class="nav-item">
                  <span class="navbar-text me-3">
                    Olá, <%= current_user.email %>
                  </span>
                </li>

                <%# --- LÓGICA DE LINKS POR PAPEL --- %>
                <% if current_user.admin? %>
                  <li class="nav-item">
                    <%= link_to "Painel Admin", admin_root_path, class: "nav-link" %>
                  </li>
                <% elsif current_user.moderator? %>
                  <li class="nav-item">
                    <%= link_to "Meu Dashboard", moderator_root_path, class: "nav-link" %>
                  </li>
                <% end %>
                <li class="nav-item">
                    <%= link_to "Questionários", questionnaires_path, class: "nav-link" %>
                </li>
                 <%# --- FIM DA LÓGICA DE LINKS --- %>

                <li class="nav-item">
                  <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-outline-danger" %>
                </li>
              <% else %>
                <li class="nav-item">
                  <%= link_to "Login", new_user_session_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                  <%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
                </li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <%# --- FIM DA BARRA DE NAVEGAÇÃO --- %>

    <main class="container mt-4">
      <%# Mensagens de alerta e notice com estilo Bootstrap %>
      <% if notice.present? %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
      <% if alert.present? %>
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>

      <%# Conteúdo da página é inserido aqui %>
      <%= yield %>
    </main>
  </body>
</html>

